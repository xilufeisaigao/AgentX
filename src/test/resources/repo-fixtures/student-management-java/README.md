# Student Management Java Fixture

This fixture is the baseline repository for strict real workflow evaluation.
It intentionally contains only a tiny in-memory student service so the agent can
expand validation, CRUD behavior, and regression coverage without re-scaffolding
the whole repository.
