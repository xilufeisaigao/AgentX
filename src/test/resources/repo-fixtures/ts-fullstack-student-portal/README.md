# TS Fullstack Student Portal Fixture

This fixture follows the `apps/web + apps/api + packages/shared` monorepo shape.
It is intentionally small and dependency-light so real workflow evaluation can
focus on planning, contract alignment, and shared/api/web integration evidence.
